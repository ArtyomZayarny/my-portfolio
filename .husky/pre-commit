#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Проверка TypeScript ошибок
echo "🔍 Проверка TypeScript..."
npm run type-check || {
  echo "❌ Обнаружены ошибки TypeScript. Коммит отменен."
  exit 1
}

# Проверка линтера на измененных файлах
echo "🔍 Проверка линтера..."
npx lint-staged || {
  echo "❌ Обнаружены ошибки линтера. Коммит отменен."
  exit 1
}

echo "✅ Все проверки пройдены успешно!"
